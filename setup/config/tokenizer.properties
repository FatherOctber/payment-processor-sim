tokenizer.kafkaInputTopic=data-input
tokenizer.kafkaOutputTopic=data-output
tokenizer.partitions=1
tokenizer.cipherAlgorithm=AES

kafka.bootstrap.servers=localhost:9092
kafka.streams.appid=tokenizer-stream
redis.host=localhost
redis.port=6379
server.port=8082